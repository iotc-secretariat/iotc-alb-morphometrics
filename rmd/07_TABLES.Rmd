
# Tables

```{r SamplingDesignTableSourceYear, tab.cap = "Number of albacore samples (N) by data source and year along with the range of morphometric measurements for fork length (FL; cm) and round weight (RD; kg)"}
SAMPLING_DESIGN_TABLE_SOURCE_YEAR_FT
```

\pagebreak

```{r SamplingDesignTableSourceFisheryGroup, tab.cap = "Number of albacore samples (N) by data source and fishery group along with the range of morphometric measurements for fork length (FL; cm) and round weight (RD; kg)"}
SAMPLING_DESIGN_TABLE_SOURCE_FISHERY_GROUP_FT
```

\pagebreak


```{r AnovaTable, tab.cap = "Summary table of analysis of variance of the round weight in tropical tuna.", eval = FALSE}
ALB_FL_RD_ANOVA_FT
```

